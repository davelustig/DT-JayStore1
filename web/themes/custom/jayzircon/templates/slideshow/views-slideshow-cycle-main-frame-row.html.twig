{#
/**
 * @file
 * Default theme implementation for an individual views slideshow cycle slide.
 *
 * Available variables:
 * - classes: Classes to apply to the element.
 * - count: The slide number.
 * - rendered_items: Rendered items for slide.
 * - vss_id: The slideshow's id.
 *
 * @see template_preprocess_views_slideshow_()
 *
 * @ingroup vss_templates
 *
 * MODIFICATIONS: This templated was modified as follows - the style width has been hardcoded to auto
 * so the slide is cropped and centered correctly as the page is resized.
 */
#}
<div id="views_slideshow_cycle_div_{{ vss_id }}_{{ count }}" style="width: auto" {{ attributes.addClass(classes) }}>
  {% for rendered_item in rendered_items %}
    {{ rendered_item }}
  {% endfor %}
</div>
